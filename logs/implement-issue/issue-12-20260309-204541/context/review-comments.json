What's done well: the `saving` type change from `number` to `{ tokens, label }` object is applied consistently across all recommendation pushes and the single render site (line 1310). The `null` guard (`rec.saving != null`) works correctly with the new object type. The `dateRangeMonths` helper has correct defensive handling for missing/partial date ranges (returns 1 as safe default). The Opus→Sonnet token-savings formula is mathematically sound since input and output token price ratios are identical across tiers (1:5), making the mixed-token approximation valid.
The token savings reformatting work (token-based display instead of dollar estimates, `recFmtTokenSaving`, `dateRangeMonths`, `recCalcPriceRatioSavings`) is well-executed. The saving property schema change from primitive number to `{tokens, label}` is consistently applied across all producers and consumers. The tooltip additions follow the established CSS pattern correctly. The single blocking issue is the 5 orphaned canvas elements in the pipeline section with no rendering implementation.
